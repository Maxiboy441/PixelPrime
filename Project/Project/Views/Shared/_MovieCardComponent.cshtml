@*
    string argument 1: movie poster
    string argument 2: movie rating
    string argument 3: movie title

    Usage:
    @await Html.PartialAsync("_MovieCardComponent", Tuple.Create(movie.Poster, movie.Ratings, movie.Title, modie.Id))
*@

@model Tuple<string, string, string, string>

<div class="card bg-secondary-color" style="width: 18rem;">
    <div class="card-img-container">
        <a href="/movies/@Model.Item4">
            @if (!string.IsNullOrEmpty(Model.Item1) && Model.Item1 != "N/A")
            {
                <img class="card-img-top" src="@Model.Item1" alt="@Model.Item3">
            }
            else
            {
                <img class="card-img-top" src="@Url.Content("~/images/poster_not_found.png")" alt="Poster not found">
            }
        </a>
    </div>
    <div class="card-body d-flex flex-column">
        <p class="card-text"><i class="fa-solid fa-star" style="color: #FFD43B;"></i><span class="rate">@Model.Item2</span></p>
        <h6 class="card-title mb-3"><a href="#">@Model.Item3</a></h6>
        <div class="mt-auto text-center">
            @await Html.PartialAsync("_PrimaryButton", Tuple.Create("See Movie", "style-2", "", $"href=/movies/{@Model.Item4}", "a"))
        </div>
    </div>
</div>
